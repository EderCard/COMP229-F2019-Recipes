@model IEnumerable<Recipe>
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>RecipeList</title>
    <link rel="stylesheet" href="/lib/boostrap/css/bootstrap.min.css" />
    <link rel="stylesheet" href="/css/styles.css" />
</head>
<body>
    <div id="allPage">
        <header class="text-center">
            <h1>Best Recipes</h1>
            <h2>Take a look on your recipe list</h2>
            <a class="btn btn-outline-primary" asp-action="Index">Home</a>
            <a class="btn btn-primary" asp-action="RecipeList">Recipe List</a>
            <a class="btn btn-outline-primary" asp-action="AddRecipe">Add</a>
        </header>
        <br />
        <table class="table table-sm table-bordered table-hover">
            <thead class="thead-light">
                <tr>
                    <th>Id</th>
                    <th>Name</th>
                    <th>Category</th>
                    <th>Rate</th>
                    <th></th>
                </tr>
            </thead>
           
            <tbody>
                @foreach (Recipe r in Model)
                 {
                <tr>
                    <td>@r.RecipeId</td>
                    <td>@r.Name</td>
                    <td>@r.Category</td>
                    <td>@r.Rate</td>
                    <td><a class="btn btn-primary" asp-action="ViewRecipe" asp-route-id=@r.RecipeId>View</a>
                    <a class="btn btn-primary" asp-action="ReviewRecipe" asp-route-id=@r.RecipeId>Review</a></td>
                </tr>
                 }
            </tbody>
        </table>
    </div>
</body>
</html>
